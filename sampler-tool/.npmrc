; vite-plugin-pwa@1.2.0 declares peer vite ^3..^7 but we run vite 8.
; The plugin works fine on vite 8 — the peer range just lags.
; This affects local install AND CI (Cloudflare Pages npm clean-install).
legacy-peer-deps=true
